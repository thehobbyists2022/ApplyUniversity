name: Supabase Keep-Alive Ping

on:
  schedule:
    - cron: '0 4 */3 * *'
  workflow_dispatch:

jobs: 
  ping-supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase Auth to generate DB activity
        run: |
          echo "Pinging Supabase Auth API to trigger a database query..."
          curl -s -o /dev/null -w "%{http_code}\n" -X POST "https://whounbqjinqxkkxunfkx.supabase.co/auth/v1/token?grant_type=password" \
            -H "apikey: sb_publishable_l1Y86_jMvQ578wgi1yh1TQ_dMLWWPLH" \
            -H "Content-Type: application/json" \
            -d '{"email":"ping-keepalive@example.com","password":"ping-keepalive-123"}'
